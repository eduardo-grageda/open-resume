# name: Desktop Release

# on:
#   push:
#     tags: ['v*']
#   workflow_dispatch:

# jobs:
#   build:
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           - os: ubuntu-latest
#             target: x86_64-unknown-linux-gnu
#             artifact: open-resume_${{ github.ref_name }}_amd64
#             target_rust: x86_64-unknown-linux-gnu
#           - os: windows-latest
#             target: x86_64-pc-windows-msvc
#             artifact: open-resume_${{ github.ref_name }}_x64
#             target_rust: x86_64-pc-windows-msvc

#     runs-on: ${{ matrix.os }}

#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Setup Rust
#         uses: dtolnay/rust-toolchain@stable
#         with:
#           targets: ${{ matrix.target_rust }}

#       - name: Install Linux system deps
#         if: runner.os == 'Linux'
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y \
#             libwebkit2gtk-4.1-dev libgtk-3-dev libcairo2-dev \
#             libpango1.0-dev libgdk-pixbuf-2.0-dev libsoup-3.0-dev \
#             libjavascriptcoregtk-4.1-dev librsvg2-dev \
#             libayatana-appindicator3-dev pkg-config

#       - name: Install Python deps
#         run: |
#           python -m venv venv
#           source venv/bin/activate || venv\Scripts\Activate.ps1
#           pip install -r backend/requirements.txt pyinstaller
#         shell: bash

#       - name: Build backend (PyInstaller)
#         shell: bash
#         run: |
#           if [ "${{ runner.os }}" = "Windows" ]; then
#             venv/Scripts/python.exe -m pip install pyinstaller
#             venv/Scripts/pyinstaller.exe --clean --noconfirm backend/open-resume-backend.spec
#           else
#             source venv/bin/activate
#             pip install pyinstaller
#             pyinstaller --clean --noconfirm backend/open-resume-backend.spec
#           fi
#           mkdir -p src-tauri/binaries
#           if [ "${{ runner.os }}" = "Windows" ]; then
#             cp dist/open-resume-backend.exe "src-tauri/binaries/open-resume-backend-${{ matrix.target }}.exe"
#           else
#             cp dist/open-resume-backend "src-tauri/binaries/open-resume-backend-${{ matrix.target }}"
#             chmod +x "src-tauri/binaries/open-resume-backend-${{ matrix.target }}"
#           fi

#       - name: Build frontend
#         run: |
#           cd frontend
#           npm ci
#           npm run build
#           cd ..

#       - name: Build Tauri
#         uses: tauri-apps/tauri-action@v0
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         with:
#           beforeBuildCommand: "true"
#           args: --target ${{ matrix.target_rust }}

#       - name: Upload artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: ${{ matrix.artifact }}
#           path: src-tauri/target/${{ matrix.target_rust }}/release/bundle/